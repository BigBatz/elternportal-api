# ------------------------------------------------------------
# Elternportal Tooling – Beispielkonfiguration
# Ablegen als tooling/.env und mit eigenen Werten füllen.
# ------------------------------------------------------------

# Elternportal-Konfiguration (ESM/JS). Standard verweist auf eine eigene config.prod.js
EP_CONFIG=../tooling/config.prod.js
# Optionale Filter: Schulen (Shortcode), Kinder-IDs, Namen (kommasepariert)
EP_SCHOOL=
EP_KID=
EP_KID_NAME=

# CalDAV-Ziel (z. B. iCloud) für manuelle CLI-Aufrufe
ICLOUD_CAL_URL=
ICLOUD_USER=
ICLOUD_PASS=
# Optional: UID-Präfix (falls mehrere Kalender mit unterschiedlichen IDs befüllt werden)
CAL_UID_PREFIX=
# Optional: Standard-Quelle für den Sync (vertretung, schulaufgaben, termine)
CAL_SOURCE=vertretung
# Optional: Minuten, um die das Perioden-Ende gekürzt wird (iOS Darstellung)
PERIOD_END_OFFSET_MINUTES=1
REMINDER_MINUTES=15
ORGANIZER_EMAIL=
ORGANIZER_CN=Elternportal Automation

# Ordner, in dem Exporter die JSON-Dateien ablegen
OUTPUT_DIR=../data
