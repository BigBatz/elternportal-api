# Pfad zur Elternportal-Konfiguration (ESM/JS Modul)
EP_CONFIG=../examples/config.js
# Optional: Schul-Kürzel filtern (kommasepariert)
EP_SCHOOL=
# Optional: Kinder-IDs filtern (kommasepariert)
EP_KID=
# Optional: Kinder-Namen filtern (kommasepariert, case-insensitive)
EP_KID_NAME=
# Wohin der Export JSON-Dateien schreibt
OUTPUT_DIR=/data
# Intervall für den Endlossync in Sekunden (Standard 1800 = 30 Minuten)
SYNC_INTERVAL=1800
